spring:
  application:
    name: register-users-employees
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: ${DB_DIALECT}
server:
  port: ${PORT:8081}